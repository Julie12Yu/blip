# Copy to .env and fill in values. Use with Docker: docker run --env-file .env ...

# Supabase (used by app, api/cron, and scripts)
SUPABASE_URL=
SUPABASE_KEY=

# OpenAI / LLM (used by app)
GPT_KEY=

# Optional: news APIs (used by app)
GUARDIAN_API_KEY=
NYT_API_KEY=

# Old FastAPI deployment (old_deployment_stuff/)
TOGETHER_API_KEY=
