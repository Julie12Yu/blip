{
  "version": 2,
  "buildCommand": "cd blip-react && CI=false npm install && CI=false npm run build",
  "outputDirectory": "blip-react/build",
  "crons": [
    { "path": "/api/cron", "schedule": "0 0 * * 1" }
  ]
}